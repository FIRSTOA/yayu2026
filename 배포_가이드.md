# 배포 가이드 — Supabase + GitHub + Vercel

야유회 웹앱을 **인터넷 주소로 배포**하고 **여러 사람이 함께 편집**하도록 만드는 순서입니다.
개발 지식 없이 클릭만으로 따라 할 수 있게 정리했습니다.

> 지금 상태(계정 없이): `index.html`을 브라우저로 열면 **바로 편집 가능**합니다.
> 단, 수정 내용이 **내 브라우저에만** 저장됩니다(다른 사람과 공유 X).
> 아래 3단계를 마치면 **인터넷 공유 + 실시간 공동 편집**이 됩니다.

---

## 1단계 · Supabase (데이터 저장소 만들기)

1. https://supabase.com 접속 → **Start your project** → GitHub 또는 이메일로 가입(무료).
2. **New project** 클릭 → 이름(예: `yayu-2026`), 비밀번호(아무거나, 메모해두기), 지역(**Northeast Asia (Seoul)** 권장) 선택 → **Create**. (1~2분 대기)
3. 왼쪽 메뉴 **SQL Editor** → **New query** → 이 폴더의 **`supabase_schema.sql`** 파일 내용을 전부 복사해 붙여넣기 → **Run**.
   → "Success" 나오면 테이블 5개 + 데이터가 자동 생성됩니다.
4. 왼쪽 메뉴 **Project Settings(톱니) → API** 로 이동해서 두 값을 복사:
   - **Project URL** (예: `https://abcd1234.supabase.co`)
   - **anon public** 키 (`eyJ...` 로 시작하는 긴 문자열)
5. 이 폴더의 **`js/config.js`** 를 열어 붙여넣기:
   ```js
   window.SUPABASE_CONFIG = {
     url: "https://abcd1234.supabase.co",   // ← Project URL
     key: "eyJhbGciOiJIUzI1NiI...",         // ← anon public key
   };
   ```
6. 저장 후 `index.html`을 다시 열면 홈 상단이 **☁️ 클라우드 저장** 으로 바뀝니다.
   → 이제 수정 내용이 Supabase에 저장되고, 누가 접속해도 같은 데이터를 봅니다.

---

## 2단계 · GitHub (코드 올리기)

1. https://github.com 가입/로그인 → 오른쪽 위 **＋ → New repository**.
2. 이름(예: `yayu-2026`), **Private(비공개) 권장** 선택 → **Create repository**.
3. 코드를 올리는 가장 쉬운 방법 — **웹에서 드래그 업로드**:
   - 생성된 저장소 화면에서 **uploading an existing file** 링크 클릭.
   - 이 폴더의 파일들을 끌어다 놓기:
     `index.html, schedule.html, attendees.html, budget.html, checklist.html`,
     `css/` 폴더, `js/` 폴더.
   - ⚠️ **`26년_야유회_모음.xlsx` 와 `야유회_백업.json` 은 올리지 마세요**(개인정보).
   - **Commit changes** 클릭.

> 참고: Git이 익숙하면 이 폴더에서 `git init → add → commit → push` 로 올려도 됩니다.
> (`.gitignore` 에 엑셀 원본이 이미 제외되도록 설정돼 있습니다.)

---

## 3단계 · Vercel (실제 주소로 배포)

1. https://vercel.com → **Sign up** → **Continue with GitHub**.
2. **Add New… → Project** → 방금 만든 `yayu-2026` 저장소 **Import**.
3. 설정은 손댈 것 없음(정적 사이트) → **Deploy** 클릭. (1분 내 완료)
4. 완료되면 `https://yayu-2026.vercel.app` 같은 **주소**가 생깁니다. 끝!
   - 이후 GitHub에 파일을 수정해 올릴 때마다 자동으로 다시 배포됩니다.

---

## 🔒 개인정보 보호 (꼭 읽어주세요)

이 데이터에는 **직원 실명 · 스탭 전화번호 · 개인 사유(육아/건강 등)** 가 들어 있습니다.
Vercel 주소는 기본적으로 **주소를 아는 누구나 접속**할 수 있으니, 아래 중 하나를 권합니다.

- **가장 쉬움 — Vercel 비밀번호 잠금**: Vercel 프로젝트 → **Settings → Deployment Protection → Password Protection** 켜기 (Pro 기능).
- **무료 대안**: 배포하지 말고 **사내에서만** `index.html`을 열어 쓰거나, 파일을 사내 공유 폴더/그룹웨어에 두기.
- **본격적으로**: Supabase 로그인(Auth)을 붙여 로그인한 사람만 보게 하기(설정이 더 필요 — 원하시면 도와드릴게요).

또한 Supabase의 `supabase_schema.sql` 마지막 부분은 "누구나 읽기/쓰기" 정책이라,
**anon 키가 공개되면 데이터가 수정될 수 있습니다.** 민감하면 로그인 기반으로 바꾸세요.

---

## 자주 겪는 문제

- **홈에 아직 💾 브라우저 저장으로 보임** → `js/config.js` 의 url/key 오타 확인, 저장 후 새로고침(Ctrl+F5).
- **클라우드인데 목록이 비어 있음** → 1단계 3번(SQL 실행)을 안 했거나 실패한 경우. SQL Editor에서 다시 Run.
- **저장이 안 됨(권한 오류)** → `supabase_schema.sql` 의 RLS 정책(맨 아래)을 실행했는지 확인.
- **데이터를 원본으로 되돌리고 싶음(로컬 모드)** → 홈 화면 → **↩️ 엑셀 원본으로 초기화**.
